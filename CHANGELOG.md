# 更新记录

## 2.0.1 - 2026-06-26

- 插件改名为 `astrbot_plugin_majsoul_stats`。之前跟 kterna 那个 `astrbot_plugin_majsoul` 同名，装到一起会因为目录重名直接报错，索性让一步改个名。
- 指令、配置、用法都没变，老用户照常用就行，重装时记得搜带 `_stats` 的这个。

## 2.0.0 - 2026-06-23

- 适配 AstrBot 4.25，移除已弃用的插件注册方式。
- 查询结果改为结构化数据，失败信息不会再交给模型点评。
- 完整显示一至四位率、放铳率和平均顺位。
- 优先匹配完整昵称，同名或模糊结果会提示用户补充昵称。
- 复用 HTTP 连接，并为超时、限流和服务端错误增加重试。
- 缓存增加过期时间和数量上限，并合并相同玩家的并发查询。
- 牌谱功能只返回真实识别结果，不再生成不存在的牌局数据。
- 增加 `/雀魂帮助`、自动化测试和 GitHub Actions 检查。
